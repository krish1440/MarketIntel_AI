---
wave: 1
depends_on: []
files_modified: [models/fusion_model.py, models/train_fusion.py]
autonomous: true
requirements: [INTL-03]
---

# 04-01-PLAN: XGBoost Fusion Model Implementation

This plan implements the meta-learner that fuses price and sentiment signals.

## Tasks

<task>
<read_first>
- .planning/research/ARCHITECTURE.md
- models/price_model.py
- models/sentiment_model.py
</read_first>
<action>
Create `models/fusion_model.py`.
- Define a `MultimodalFusion` class.
- Implement methods to load the LSTM and DistilBERT experts.
- Implement the XGBoost model wrapper.
- Define the feature vector structure (LSTM output, sentiment avg, etc.).
</action>
<acceptance_criteria>
- `models/fusion_model.py` exists.
- Successfully defines the fusion architecture.
</acceptance_criteria>
</task>

<task>
<read_first>
- models/preprocess.py
- ingestion/update_sentiment.py
</read_first>
<action>
Create `models/train_fusion.py`.
- Implement a data generator that aligns historical prices and news articles by date.
- Generate a "fusion dataset" where each row has features from both experts.
- Train the XGBoost model.
- Save the model to `models/checkpoints/fusion_model.json`.
</action>
<acceptance_criteria>
- `models/train_fusion.py` exists and runs.
- Produces a valid XGBoost checkpoint file.
</acceptance_criteria>
</task>

## Verification Criteria
1. `fusion_model.json` is created.
2. The fusion model achieves better validation accuracy/MSE than the LSTM alone (simulated if data is sparse).
3. The model can be loaded and used for inference.

## must_haves
- [ ] Late fusion strategy implemented.
- [ ] Feature alignment logic for disparate data sources (time-series vs text).
- [ ] XGBoost model for final signal generation.
