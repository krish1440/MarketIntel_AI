---
wave: 2
depends_on: [04-01]
files_modified: [intelligence/prediction_service.py, intelligence/incremental_learner.py]
autonomous: true
requirements: [SYS-01]
---

# 04-02-PLAN: Prediction Service and Incremental Learning

This plan implements the unified interface for intelligence and the incremental learning loop.

## Tasks

<task>
<read_first>
- models/fusion_model.py
</read_first>
<action>
Create `intelligence/prediction_service.py`.
- Implement a `PredictionService` class.
- Function `get_signal(ticker)`: orchestrates data fetching, expert inference, and fusion prediction.
- Return a structured response: `{ ticker, signal: "BUY/SELL/HOLD", confidence, price_forecast, sentiment_score }`.
</action>
<acceptance_criteria>
- `intelligence/prediction_service.py` exists.
- Provides a clean API for the future dashboard.
</acceptance_criteria>
</task>

<task>
<read_first>
- models/train_fusion.py
</read_first>
<action>
Create `intelligence/incremental_learner.py`.
- Implement a script that checks for "new" completed days (where actual close is now known).
- Compare actual vs predicted signals.
- Use XGBoost's `xgb_model.fit(X, y, xgb_model=prior_model)` to incrementally update the meta-learner.
</action>
<acceptance_criteria>
- `intelligence/incremental_learner.py` exists.
- Successfully performs a model update on a sample batch.
</acceptance_criteria>
</task>

## Verification Criteria
1. `PredictionService` returns a consistent Buy/Sell/Hold signal for a given ticker.
2. `IncrementalLearner` successfully updates the `fusion_model.json` file.
3. The system can handle stocks with varying amounts of available news data.

## must_haves
- [ ] Unified prediction API.
- [ ] Incremental update logic for the meta-learner.
- [ ] Robust handling of missing news signals (fall back to price-only if needed).
