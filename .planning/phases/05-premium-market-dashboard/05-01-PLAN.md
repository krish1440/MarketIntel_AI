---
wave: 1
depends_on: []
files_modified: [api/main.py, requirements.txt]
autonomous: true
requirements: [SYS-03]
---

# 05-01-PLAN: API Backend Implementation

This plan implements the REST API that connects the intelligence engine to the frontend.

## Tasks

<task>
<read_first>
- intelligence/prediction_service.py
- ingestion/poll_prices.py
</read_first>
<action>
Create `api/main.py` using FastAPI.
- Implement `/api/stocks` to return all stocks with their latest price and 24h change.
- Implement `/api/stocks/{ticker}` to return detailed price history and technical indicators.
- Implement `/api/predict/{ticker}` using `PredictionService.get_signal()`.
- Implement `/api/news/{ticker}` to return recent articles with sentiment scores.
- Enable CORS for local development.
</action>
<acceptance_criteria>
- `api/main.py` exists.
- All endpoints return valid JSON data from the database.
</acceptance_criteria>
</task>

<task>
<read_first>
- requirements.txt
</read_first>
<action>
Update `requirements.txt` to include `fastapi` and `uvicorn`.
</action>
<acceptance_criteria>
- `requirements.txt` is updated.
</acceptance_criteria>
</task>

## Verification Criteria
1. `uvicorn api.main:app --reload` starts the server successfully.
2. `/docs` (Swagger) is accessible.
3. `/api/stocks` returns the list of seeded stocks.

## must_haves
- [ ] FastAPI backend bridged to the DB and Intelligence engine.
- [ ] CORS support for frontend integration.
- [ ] Robust error handling for invalid tickers.
