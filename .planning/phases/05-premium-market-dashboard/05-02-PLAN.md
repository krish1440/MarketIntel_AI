---
wave: 2
depends_on: [05-01]
files_modified: [dashboard/*]
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]
---

# 05-02-PLAN: Frontend Dashboard Implementation

This plan creates the premium Moneycontrol-like dashboard using Next.js and the Stitch design system.

## Tasks

<task>
<read_first>
- .planning/phases/05-premium-market-dashboard/05-CONTEXT.md
</read_first>
<action>
Initialize the Next.js project in the `dashboard/` directory.
- Use `npx create-next-app@latest dashboard --typescript --tailwind --eslint`.
- Configure the basic theme with indigo/slate color palette.
</action>
<acceptance_criteria>
- `dashboard/` directory contains a working Next.js project.
</acceptance_criteria>
</task>

<task>
<read_first>
- StitchMCP tools documentation
</read_first>
<action>
Use Stitch to generate the core screens.
- Use `create_project` to start a Stitch project.
- Use `generate_screen_from_text` to create the "Market Overview" and "Stock Detail" screens with a premium, Moneycontrol-like prompt.
- Apply a design system using `create_design_system` (Indigo Midnight theme).
</action>
<acceptance_criteria>
- Stitch project and screens are created and visible in the UI.
- Design system is applied.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/main.py
</read_first>
<action>
Implement API integration in the Next.js app.
- Create a `lib/api.ts` utility to fetch data from the FastAPI backend.
- Wire the Stitch-generated components to real data.
- Implement the "AI Signal" badge with confidence level display.
- Add "Sentiment Gauge" and "News Feed" components.
</action>
<acceptance_criteria>
- Dashboard shows real-time data from the backend.
- Charts and news feeds are functional.
</acceptance_criteria>
</task>

## Verification Criteria
1. `npm run dev` starts the dashboard.
2. The UI matches the "premium" aesthetic (Moneycontrol-like).
3. Live data from the backend is visible in the UI.

## must_haves
- [ ] Premium design using Stitch.
- [ ] Real-time data integration with FastAPI.
- [ ] Interactive charts and AI signal visualization.
