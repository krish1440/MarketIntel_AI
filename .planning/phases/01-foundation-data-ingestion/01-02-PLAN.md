---
wave: 2
depends_on: [01-01]
files_modified: [ingestion/poll_prices.py, requirements.txt]
autonomous: true
requirements: [DATA-01]
---

# 01-02-PLAN: Live Polling Service Implementation

This plan implements the Python service that polls the NSE/BSE websites for live quotes and stores them in the database.

## Tasks

<task>
<read_first>
- .planning/research/STACK.md
</read_first>
<action>
Create `requirements.txt` with necessary dependencies.
- `nsepython`
- `bsedata`
- `sqlalchemy`
- `psycopg2-binary`
- `python-dotenv`
</action>
<acceptance_criteria>
- `requirements.txt` contains all specified libraries.
</acceptance_criteria>
</task>

<task>
<read_first>
- db/schema.py
- .planning/phases/01-foundation-data-ingestion/01-CONTEXT.md
</read_first>
<action>
Create `ingestion/poll_prices.py` to fetch and store live quotes.
- Implement `fetch_nse_quote(symbol)` using `nsepython`.
- Implement `fetch_bse_quote(symbol)` using `bsedata`.
- Create a loop that iterates through a list of 10 symbols (e.g., RELIANCE, TCS, HDFCBANK).
- Store the results in the `live_quotes` table using the SQLAlchemy models from `db/schema.py`.
- Implement a sleep interval (e.g., 60 seconds) between polls.
- Use basic error handling to skip failed fetches and log the error.
</action>
<acceptance_criteria>
- `ingestion/poll_prices.py` correctly imports the DB schema.
- Script handles both NSE and BSE fetching logic.
- Data is successfully inserted into the `live_quotes` table.
</acceptance_criteria>
</task>

## Verification Criteria
1. `python ingestion/poll_prices.py` runs without errors.
2. Database `live_quotes` table contains new entries after the script runs.
3. The `stock_id` correctly maps to the entries in the `stocks` table.

## must_haves
- [ ] Live price data can be fetched from both exchanges.
- [ ] Data is stored in the local database with timestamps.
- [ ] Basic error handling prevents the service from crashing on single-stock failures.
