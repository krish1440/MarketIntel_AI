---
wave: 1
depends_on: []
files_modified: [models/price_model.py, models/utils.py, models/preprocess.py]
autonomous: true
requirements: [INTL-01, INTL-04]
---

# 03-01-PLAN: LSTM Price Model Implementation

This plan implements the LSTM model for price prediction, optimized for Intel Iris Xe.

## Tasks

<task>
<read_first>
- .planning/research/STACK.md
- .planning/phases/03-model-experts-price-text/03-CONTEXT.md
</read_first>
<action>
Create `models/preprocess.py`.
- Implement technical indicator calculation (SMA, RSI) using `pandas` or `ta-lib` (if available).
- Implement data scaling using `MinMaxScaler` from `sklearn`.
- Create a function to generate windowed sequences for LSTM (e.g., 60-day window).
</action>
<acceptance_criteria>
- `models/preprocess.py` exists.
- Successfully generates X and y arrays from database records.
</acceptance_criteria>
</task>

<task>
<read_first>
- models/preprocess.py
</read_first>
<action>
Create `models/price_model.py`.
- Define a `StockLSTM` class using `torch.nn.Module`.
- Implement the forward pass.
- Include a helper function `train_model()` that uses `intel_extension_for_pytorch` (IPEX) to optimize the model.
- Save the trained weights to `models/checkpoints/price_model.pth`.
</action>
<acceptance_criteria>
- `models/price_model.py` exists.
- Model is successfully optimized with IPEX.
- Training loop runs and produces a `.pth` file.
</acceptance_criteria>
</task>

## Verification Criteria
1. `price_model.pth` is created.
2. Training loss decreases over epochs.
3. Model can perform a forward pass on sample data.

## must_haves
- [ ] LSTM architecture implemented in PyTorch.
- [ ] Intel IPEX optimization enabled.
- [ ] Technical indicators included in the feature set.
