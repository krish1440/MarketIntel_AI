---
wave: 2
depends_on: [03-01]
files_modified: [models/sentiment_model.py, ingestion/update_sentiment.py]
autonomous: true
requirements: [INTL-02, INTL-04]
---

# 03-02-PLAN: DistilBERT Sentiment Model Implementation

This plan implements the sentiment analysis model for processing stock news.

## Tasks

<task>
<read_first>
- .planning/research/STACK.md
- .planning/phases/03-model-experts-price-text/03-CONTEXT.md
</read_first>
<action>
Create `models/sentiment_model.py`.
- Load `distilbert-base-uncased-finetuned-sst-2-english` using Hugging Face `transformers`.
- Implement a `get_sentiment(text)` function that returns a score between -1 and 1.
- Optimize the model for inference using IPEX if applicable (or just use standard Torch optimization).
</action>
<acceptance_criteria>
- `models/sentiment_model.py` exists.
- Correctly classifies sample sentences (e.g., "Market is crashing" as negative).
</acceptance_criteria>
</task>

<task>
<read_first>
- ingestion/news_aggregator.py
- models/sentiment_model.py
</read_first>
<action>
Create `ingestion/update_sentiment.py`.
- Fetch articles from `news_articles` where `sentiment_score` is NULL.
- Batch process articles through the sentiment model.
- Update the `news_articles` table with the computed scores.
</action>
<acceptance_criteria>
- `ingestion/update_sentiment.py` exists and runs.
- `news_articles` table is populated with sentiment scores.
</acceptance_criteria>
</task>

## Verification Criteria
1. Sentiment scores are stored in the database for all processed articles.
2. The aggregator/updater script can be run on new articles without re-processing old ones.
3. Sentiment scores reflect the content (negative for bad news, positive for good news).

## must_haves
- [ ] DistilBERT model integrated for text processing.
- [ ] Database update logic for sentiment scores.
- [ ] Batch processing to optimize performance.
